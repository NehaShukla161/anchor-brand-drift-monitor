# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# Genblaze — pipeline orchestration + B2/S3 storage
genblaze-core
genblaze-s3
genblaze-gmicloud
genblaze-openai

# Embeddings for drift scoring
open_clip_torch==2.24.0
torch==2.3.1
pillow==10.4.0
numpy==1.26.4

# B2 direct access (reference kit + drift log, outside the Genblaze sink)
boto3==1.34.162
